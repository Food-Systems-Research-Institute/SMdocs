% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_reactable.R
\name{get_reactable}
\alias{get_reactable}
\title{Create a customized reactable table}
\usage{
get_reactable(
  df,
  sortable = TRUE,
  resizable = TRUE,
  filterable = TRUE,
  searchable = TRUE,
  pagination = TRUE,
  bordered = TRUE,
  wrap = TRUE,
  rownames = FALSE,
  onClick = "select",
  striped = TRUE,
  defaultPageSize = 10,
  showPageSizeOptions = TRUE,
  highlight = TRUE,
  style = list(fontSize = "14px"),
  compact = TRUE,
  fullWidth = TRUE,
  ...
)
}
\arguments{
\item{df}{A data frame to display in the reactable table.}

\item{sortable}{Logical indicating whether columns should be sortable.
Default is TRUE.}

\item{resizable}{Logical indicating whether columns should be resizable.
Default is TRUE.}

\item{filterable}{Logical indicating whether columns should be filterable.
Default is TRUE.}

\item{searchable}{Logical indicating whether the table should have a global
search box. Default is TRUE.}

\item{pagination}{Logical indicating whether to enable pagination.
Default is TRUE.}

\item{bordered}{Logical indicating whether to show table borders.
Default is TRUE.}

\item{wrap}{Logical indicating whether to wrap text in cells.
Default is TRUE.}

\item{rownames}{Logical indicating whether to show row names.
Default is FALSE.}

\item{onClick}{Character string specifying click behavior. Options include
'expand', 'select', or NULL. Default is 'select'.}

\item{striped}{Logical indicating whether to show striped rows.
Default is TRUE.}

\item{defaultPageSize}{Numeric specifying the default number of rows per page.
Default is 10.}

\item{showPageSizeOptions}{Logical indicating whether to show page size options.
Default is TRUE.}

\item{highlight}{Logical indicating whether to highlight rows on hover.
Default is TRUE.}

\item{style}{List of CSS styles to apply to the table. Default is
list(fontSize = "14px").}

\item{compact}{Logical indicating whether to use compact styling.
Default is TRUE.}

\item{fullWidth}{Logical indicating whether the table should take full width.
Default is TRUE.}

\item{...}{Additional arguments passed to reactable().}
}
\value{
A reactable htmlwidget object that can be displayed in HTML output.
}
\description{
This function creates a reactable table with predefined styling and
functionality options. It serves as a wrapper around the reactable function
with common default settings for interactive data tables.
}
\details{
This function provides a convenient way to create consistently
styled reactable tables across an application. It sets up common
interactive features like sorting, filtering, searching, and pagination
with sensible defaults.
}
\examples{
\dontrun{
# Basic usage
get_reactable(mtcars)

# Customize options
get_reactable(
  iris,
  defaultPageSize = 5,
  searchable = FALSE,
  striped = FALSE
)

# With additional reactable arguments
get_reactable(
  df,
  columns = list(
    name = colDef(name = "Full Name"),
    age = colDef(name = "Age (years)")
  )
)
}

}
