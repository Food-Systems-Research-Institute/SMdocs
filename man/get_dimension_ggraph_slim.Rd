% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_dimension_ggraph_slim.R
\name{get_dimension_ggraph_slim}
\alias{get_dimension_ggraph_slim}
\title{Create a slim horizontal ggraph visualization for a dimension}
\usage{
get_dimension_ggraph_slim(
  csv_path = NULL,
  framework_df = NULL,
  dimension_in,
  include_metrics = FALSE,
  x_limits = c(0, 0),
  y_limits = c(-1.5, 2.1),
  leaf_font_size = 4,
  index_label_size = 0.1,
  index_font_size = 4,
  palette = "scico::batlowW",
  arrow = NULL,
  slim = FALSE
)
}
\arguments{
\item{csv_path}{Character string specifying the path to a CSV file containing
the framework data. Either this or \code{framework_df} must be provided.}

\item{framework_df}{Data frame containing the framework data. Either this or
\code{csv_path} must be provided.}

\item{dimension_in}{Character string specifying the dimension to visualize.
Will be converted to lowercase for consistency.}

\item{include_metrics}{Logical indicating whether to include metrics in the
visualization. Default is FALSE.}

\item{x_limits}{Numeric vector of length 2 specifying x-axis limits.
Default is c(0, 0).}

\item{y_limits}{Numeric vector of length 2 specifying y-axis limits.
Default is c(-1.5, 2.1).}

\item{leaf_font_size}{Numeric specifying the font size for leaf nodes.
Default is 4.}

\item{index_label_size}{Numeric specifying the label size for index labels.
Default is 0.1.}

\item{index_font_size}{Numeric specifying the font size for index labels.
Default is 4.}

\item{palette}{Character string specifying the color palette to use.
Default is 'basetheme::royal'. Use 'black' for monochrome visualization.}

\item{arrow}{Arrow specification for edges. Default is NULL.}

\item{slim}{Logical parameter for compatibility. Not used in this function.
Default is FALSE.}
}
\value{
A ggplot object containing the slim dimension ggraph visualization.
}
\description{
This function creates a simplified version of the dimension ggraph that shows
direct connections from dimension to indicators, bypassing the index level.
It's designed for cleaner visualizations when the index level is not needed.
}
\details{
This slim version creates a two-level hierarchy showing only
dimension-to-indicator connections (and optionally indicator-to-metric).
It provides a cleaner view when the index grouping level is not necessary
for the visualization. The function includes special handling for black
and white color schemes.
}
\examples{
\dontrun{
# Basic slim visualization
plot <- get_dimension_ggraph_slim(
  csv_path = "framework.csv",
  dimension_in = "economic"
)

# With metrics and custom palette
plot <- get_dimension_ggraph_slim(
  framework_df = my_data,
  dimension_in = "social",
  include_metrics = TRUE,
  palette = "black"
)
}

}
