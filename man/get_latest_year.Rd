% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_latest_year.R
\name{get_latest_year}
\alias{get_latest_year}
\title{Filter data to retain only the latest year for each variable}
\usage{
get_latest_year(
  df,
  var_col = "variable_name",
  year_col = "year",
  add_suffix = TRUE
)
}
\arguments{
\item{df}{A data frame containing the data to filter}

\item{var_col}{Character string specifying the column name containing variable names.
Default is 'variable_name'}

\item{year_col}{Character string specifying the column name containing years.
Default is 'year'}

\item{add_suffix}{Logical indicating whether to add the year as a suffix to the
variable name. Default is TRUE}
}
\value{
A data frame filtered to contain only the latest year for each variable.
If \code{add_suffix = TRUE}, variable names will have the year appended.
The year column is removed from the output.
}
\description{
This function filters a data frame so that each unique variable only has
data from its latest available year. Optionally appends the year as a suffix
to the variable name.
}
\details{
The function converts the year column to numeric, identifies the maximum
year for each variable, filters to retain only those rows, and optionally
modifies variable names to include the year suffix before removing the year column.
}
\examples{
\dontrun{
# Sample data
df <- data.frame(
  variable_name = c("var1", "var1", "var2", "var2"),
  year = c(2020, 2021, 2019, 2021),
  value = c(10, 15, 20, 25)
)

# Get latest year with suffix
get_latest_year(df)

# Get latest year without suffix
get_latest_year(df, add_suffix = FALSE)
}

}
