---
title: "Quantitative Methods for Food Systems Research"
author: "Chris Donovan"
institute: "Food Systems Research Institute"
date: '2025-11-05'
date-format: long
format:
  revealjs:
    filters:
      # - ../_extensions/andrewheiss/language-name/language-name.lua # janky extension
      - show-lang.lua # In dir of qmd file
engine: knitr
editor:
  markdown:
    wrap: 72
include-in-header:
 - text: |
     <style>
     #title-slide .title {
       font-size: 2em;
     }
     </style>
---

## Does R Work?

```{r}
#| echo: true
str(iris)
```

- Yep

## Does Python work?

```{python}
#| include: false
import sys
print(sys.executable)
```

```{python}
#| echo: true
import numpy as np
import pandas as pd
import seaborn as sns
iris = sns.load_dataset('iris')
iris.info()
```

- Yep, it does. Note jupyter argument in YAML, installing jupyter, jupyter-cache, etc, in venv, and execute daemon: false
- Also note that it is the `.Renviron` file that is setting the path to `QUARTO_PYTHON`, not `_environment`. Not even sure what `_environment` is doing.
- We also need complete file paths apparently, not relative to project
- Also note - it might be that `.Renviron` is overriding `_environment`. Worth testing this.

## Plot

```{python}
sns.boxplot(data = iris)
```
